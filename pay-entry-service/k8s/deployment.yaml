# ============================================================================
# Kubernetes Deployment for Pay Entry Service
# ============================================================================

apiVersion: apps/v1
kind: Deployment
metadata:
  name: pay-entry-service
  namespace: molam-pay
  labels:
    app: pay-entry
    component: backend
    tier: service
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: pay-entry
  template:
    metadata:
      labels:
        app: pay-entry
        component: backend
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8082"
        prometheus.io/path: "/metrics"
    spec:
      serviceAccountName: pay-entry
      containers:
      - name: pay-entry
        image: registry.molam.io/pay-entry-service:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8082
          name: http
          protocol: TCP
        env:
        - name: NODE_ENV
          value: "production"
        - name: PORT
          value: "8082"
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: pay-entry-secrets
              key: DATABASE_URL
        - name: MOLAM_ID_JWT_PUBLIC
          valueFrom:
            secretKeyRef:
              name: pay-entry-secrets
              key: MOLAM_ID_JWT_PUBLIC
        - name: SIRA_URL
          valueFrom:
            configMapKeyRef:
              name: pay-entry-config
              key: SIRA_URL
        - name: CORS_ORIGIN
          valueFrom:
            configMapKeyRef:
              name: pay-entry-config
              key: CORS_ORIGIN
        - name: RATE_LIMIT_MAX
          value: "600"
        - name: LOG_LEVEL
          value: "info"
        resources:
          requests:
            memory: "256Mi"
            cpu: "200m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8082
          initialDelaySeconds: 10
          periodSeconds: 30
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /readyz
            port: 8082
          initialDelaySeconds: 5
          periodSeconds: 10
          timeoutSeconds: 3
          failureThreshold: 2
        securityContext:
          runAsNonRoot: true
          runAsUser: 1001
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: true
          capabilities:
            drop:
            - ALL
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
          - weight: 100
            podAffinityTerm:
              labelSelector:
                matchExpressions:
                - key: app
                  operator: In
                  values:
                  - pay-entry
              topologyKey: kubernetes.io/hostname

---
apiVersion: v1
kind: Service
metadata:
  name: pay-entry-service
  namespace: molam-pay
  labels:
    app: pay-entry
spec:
  type: ClusterIP
  selector:
    app: pay-entry
  ports:
  - port: 80
    targetPort: 8082
    protocol: TCP
    name: http

---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: pay-entry
  namespace: molam-pay
